<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/ivCurrency"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="8dp"
        android:src="@drawable/img_currency"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvCurrencyTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:lineSpacingExtra="2sp"
        android:text="@string/currency_title"
        android:textAppearance="@style/dollar_tv"
        app:layout_constraintStart_toEndOf="@id/ivCurrency"
        app:layout_constraintTop_toTopOf="@id/ivCurrency" />

    <TextView
        android:id="@+id/tvCurrencySubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/currency_subtitle"
        android:textAppearance="@style/currency_subtitle_tv"
        app:layout_constraintBottom_toBottomOf="@id/ivCurrency"
        app:layout_constraintStart_toStartOf="@id/tvCurrencyTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>